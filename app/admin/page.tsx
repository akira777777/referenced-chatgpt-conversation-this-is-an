import { Clock3, Search, Wrench, CheckCircle2, PackageCheck } from "lucide-react";
import { PlaceholderTag } from "@/components/ui";
const orders = [["REP-240182", "iPhone 15 Pro", "Display", "In progress"], ["REP-240181", "MacBook Air M2", "Battery", "Diagnostics"], ["REP-240180", "Galaxy S24 Ultra", "Screen", "Ready"]];
export default function AdminPage() { return <main className="admin-page"><aside><span className="logo light"><span>R</span>REFORM</span><nav><b>Overview</b><span>Repair orders</span><span>Appointments</span><span>Devices & prices</span><span>Customers</span></nav><small>Architecture preview<br/>Authentication required before launch</small></aside><section><header><div><p className="eyebrow">ADMIN PREVIEW <PlaceholderTag/></p><h1>Service overview</h1></div><button>AR</button></header><div className="admin-stats">{[["Repairs today", "12", <Wrench/>], ["Diagnostics", "4", <Clock3/>], ["In progress", "7", <PackageCheck/>], ["Ready", "5", <CheckCircle2/>]].map(([l,v,i]) => <article key={String(l)}><span>{i}</span><p>{l}</p><b>{v}</b></article>)}</div><div className="admin-table"><div className="admin-table-head"><h2>Recent repairs</h2><label><Search/><input placeholder="Search orders"/></label></div>{orders.map(row => <div key={row[0]}>{row.map((x,i) => <span key={x} className={i === 3 ? "admin-status" : ""}>{x}</span>)}<button>Open</button></div>)}</div></section></main>; }
